Place sample datasets here. The `sample.jsonl` file demonstrates the expected
schema for training and offline evaluation:

{"event_type": "payment" | "sms" | "email", "event": { ... }, "label": 0|1}


global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'api'
    metrics_path: /metrics
    static_configs:
      - targets: ['api:8000']
  - job_name: 'consumer'
    metrics_path: /metrics
    static_configs:
      - targets: ['consumer:9000']
rule_files:
  - /etc/prometheus/alert.rules.yml
